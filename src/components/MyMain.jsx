import React, { useEffect, useState } from "react";

function MyMain(props) {
  /*   useEffect(() => {
    async function fetchData() {
      await fetchCoordinate("Roma");
      if (coordinate) {
        await fetchLocation(coordinate);
      }
    }
    fetchData();
  }, []); */

  return <div>MyMain</div>;
}

export default MyMain;
